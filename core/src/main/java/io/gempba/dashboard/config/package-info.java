/**
 * Connection-related value objects: startup config and user-edited
 * connection specs. Pure data — no SWT, no I/O, no logic beyond
 * argument parsing. Safe to depend on from any layer.
 */
package io.gempba.dashboard.config;
