/**
 * Pure display formatting — bytes, rates, CPU, durations. No SWT, no wire
 * types; just value→string helpers shared by the views so formatting lives in
 * one place rather than being copy-pasted across widgets.
 */
package io.gempba.dashboard.format;
